name: "Hello World Custom Action"
description: "Outputs a custom message"
inputs:
  message:
    description: "Message to print"
    required: false
    default: "Hello World"
outputs:
  result:
    description: "Processed message"
    value: ${{ steps.set-result.outputs.result }}
runs:
  using: "composite"
  steps:
    - name: Process message
      run: bash ${{ github.action_path }}/entrypoint.sh "${{ inputs.message }}"
      shell: bash
    - name: Return message
      id: set-result
      run: echo "result=${{ inputs.message }} (hogehoge)" >> $GITHUB_OUTPUT
      shell: bash
      
    # - run: bash ${{ github.action_path }}/entrypoint.sh "${{ inputs.message }}"
    #   shell: bash
    # - name: Process message
    #   run: |
    #     echo "Received message: ${{ inputs.message }}"
    #     echo "result=${{ inputs.message }} (processed)" >> $GITHUB_ENV
    #   shell: bash
